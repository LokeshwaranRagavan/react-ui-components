import * as React from 'react';
import { PieChartDataLabelProps } from '../base/interfaces';

/**
 * @description Component for configuring data labels in a pie chart series
 * @returns {null} - It is used only to pass the data label configuration into the pie chart through React context.
 */
export const PieChartDataLabel: React.FC<PieChartDataLabelProps> = () => {
    return null;
};

PieChartDataLabel.displayName = 'PieChartDataLabel';
